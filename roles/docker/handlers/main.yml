---
- name: (Handler) Restart rootless docker.service
  become: true
  become_user: "{{ docker_user }}"
  become_flags: --login
  ansible.builtin.systemd:
    name: docker.service
    state: restarted
    scope: user
