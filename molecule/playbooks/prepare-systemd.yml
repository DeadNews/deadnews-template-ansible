---
# Install systemd

- name: Prepare
  hosts: all

  tasks:
    - name: Install systemd
      when: ansible_os_family == "Debian"
      ansible.builtin.apt:
        name:
          - systemd
          - systemd-sysv
        state: present
        update_cache: true
